fun main() {

    // Ways to define a variable
    var first: Int = 20  // Syntax: var variableName: variableType = value
    var second = 20
    var TF: Boolean = false
    var floating: Float = 2.3f  // To define a floating type variable, we need to write (case-insensitive) f as suffix

    var age = 20
    var name = "Snehil"
    val fix = 100     // This value can't be modified

    println("Fixed value is: $fix")
    println("My name is: $name and I'm $age years old")
}